# as there are no brackets, Python will decide whether to do the addition or multiplication first
noBrackets = 1 + 2 * 3

# the brackets take priority over everything else, so the addition happens first
brackets = (1 + 2) * 3

print(noBrackets)
print(brackets)